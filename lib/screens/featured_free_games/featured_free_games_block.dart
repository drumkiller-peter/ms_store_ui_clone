import 'package:flutter/material.dart';

class FeaturedFreeGamesBlock extends StatelessWidget {
  const FeaturedFreeGamesBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green.shade600,
      height: 600,
      width: 800,
    );
  }
}
